services:

  api:
    build: 
      context: .
      dockerfile: deploy/Dockerfile.api
    image: app
    container_name: videostr
    environment:
      - API_KEY=${API_KEY}
      - PORT=:8080
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - ./assets:/app/assets
